# Chain
CHAIN=mainnet

# Database
DB_NAME=runebaseinfo
DB_USER=runebaseinfo
DB_PASS=
DB_HOST=localhost
DB_PORT=3306

# RPC (Runebase Core)
RPC_PROTOCOL=http
RPC_HOST=localhost
RPC_PORT=9948
RPC_USER=runebaseinfo
RPC_PASS=runebaseinfo

# P2P
P2P_PORT=9433

# Server (Socket.IO)
SERVER_PORT=3001
